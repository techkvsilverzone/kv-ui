# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-04-03

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->
- [2026-06-14] When fixing pre-delivery issues, the user prefers doing the frontend-only/safe items first and deferring backend-coupled money paths (payment payload changes) until the backend can change in lockstep. Document required backend changes in `API_CHANGES_AND_FIXES.md` rather than risking a broken checkout.

## Key Learnings

- **Project:** kv-silver-zone
- **Description:** Premium Silver selling eCommerce Platform
- Form validation uses zod schemas in `src/lib/validation.ts` + a `validateForm(schema, data)` helper returning `{ success, errors }` (flat field->message map). Forms keep raw `useState`, validate on submit, render inline `text-destructive` errors with `aria-invalid`. Indian phone rule: `^[6-9]\d{9}$` (tolerant of +91/spaces); PIN `^\d{6}$`.
- Protected routes are wrapped with `src/components/RequireAuth.tsx` in `App.tsx` (redirects to `/login` with `state.from`; Login returns the user there). `ErrorBoundary.tsx` wraps the route tree for render-error fallback.
- ProductDetail price breakdown prefers the server-computed `product.pricing` (`metalValue`, `makingCharge`, `ratePerGram`, `basis`) + `weightInGrams`; falls back to live `silverRateService.getTodayRate()` × weight, then a 75/25 split — display only; server stays authoritative for the charge.
- Auth is cookie-based (httpOnly) on the **web** build: `api.ts` sends `credentials: 'include'`, sets NO `Authorization` header. Logout must call `POST /auth/logout` (JS can't clear an httpOnly cookie). Only the user object is cached in `localStorage` (`kv-silver-user`). **Mobile (Capacitor) build is different:** `isMobileApp()` (in `src/lib/platform.ts`) is true, so `api.ts` attaches `Authorization: Bearer <token>` from `localStorage['kv-silver-token']`; `AuthContext` now persists the login/signup response `token` (previously ignored) and clears it on logout/401. Backend MUST accept Bearer on protected routes for the mobile app to work.
- GST percent is read from `GET /pricing-config` via `src/services/pricingConfig.ts` (default 3%) — used in `CartContext`, `Payment`, `ProductDetail`. Do NOT hardcode GST (Payment previously used 5% while Cart used 3%).
- Gift vouchers come from `GET /gift-vouchers` (`src/services/giftVoucher.ts`). Voucher cart items must carry `isGiftVoucher: true` + `giftVoucherId`; these flow into create-order and verify line items so the server prices them.
- Checkout: `POST /payments/create-order` takes `{ items:[{product,quantity,isGiftVoucher?,giftVoucherId?}], couponCode?, pincode? }` (NO client amount) and returns the authoritative `{ id, amount, currency, breakdown? }`. Never send a client-computed total as authoritative.
- SEO uses `react-helmet-async`: `<HelmetProvider>` wraps the tree in `App.tsx`; per-page `<Seo title description image? type? noindex?>` from `src/components/Seo.tsx`. IMPORTANT: any test that renders a page using `<Seo>` must wrap render in `<HelmetProvider>` or Helmet throws "Cannot read properties of undefined (reading 'add')".
- Per-endpoint frontend↔API contracts live in `docs/api/` (one md per endpoint) — update them when a request/response shape changes.
- Saved checkout address: `Payment.tsx` persists/loads the shipping address in `localStorage` under `kv-silver-address` (separate from `kv-silver-user`/`kv-silver-cart`).
- **`openapi.json` at repo root is the source of truth for API shapes** — consult it before assuming response fields. Several list/config endpoints wrap payloads in `{ status, data }` (e.g. `/gift-vouchers`, `/pricing-config`, `/products/categories`) while `/products`, `/products/{id}`, `/products/featured` return bare. GiftVoucher uses `label`+`amount`+`sortOrder`; `ProductPricing.basis` is enum `'live'|'static'`.
- Analytics/monitoring in `src/lib/analytics.ts` — `@sentry/react` + GA4, gated on `VITE_SENTRY_DSN`/`VITE_GA4_ID`, no-op when blank. `initMonitoring()` in `main.tsx`; SPA page views via `trackPageview` in `AppContent` on route change.
- Invoice download: `react-to-print` v3 `useReactToPrint({ contentRef })` in `OrderTracking.tsx`, printing the off-screen `InvoiceView` (forwardRef).
- E2E: Playwright config at root, tests in `e2e/` (vitest `include` is `src/**` so no collision). `npm run test:e2e`; first run needs `npx playwright install chromium`.
- Address book: `src/services/address.ts` (`/users/me/addresses` CRUD, bare array; `Address` has `label`/`isDefault`; `AddressInput` omits `id`). `Payment.tsx` shows a saved-address picker for authed users, prefills the default, and POSTs a new address on order success (dedup by address+pincode+phone); guests still use `localStorage` `kv-silver-address`. Order confirmation email is automatic server-side; `orderService.resendConfirmation(id)` (`POST /orders/{id}/resend-confirmation`) powers the "Resend email" button on `OrderConfirmation.tsx`.
- Password changes use `PUT /users/:userId/password` with body `{ newPassword }`, and UI should map status codes 400/403/404 to user-facing validation messages.
- Admin user-management actions can be role-gated inside `src/pages/Admin.tsx` using `role === 'admin'`, while staff may still access other admin tabs.
- Storefront theme should be fetched from public `GET /store-config` on app startup, with localStorage only as fallback.
- Admin theme management should read from `GET /admin/store-config` and update through `/admin/store-config` (PUT with POST fallback for compatibility).
- **Push notifications (mobile):** Capacitor `@capacitor/push-notifications` (FCM on Android). `src/components/PushNotifications.tsx` (mounted in `App.tsx` inside auth+router providers, renders null) requests permission, registers, shows foreground notifications as a toast, and deep-links on tap via `data.route`. Gated to `isMobileApp()` + authenticated; plugin loaded via dynamic import so it stays out of the web bundle. `src/services/push.ts` → `POST/DELETE /users/me/push-tokens` (`{ token, platform }`) — NOT yet implemented backend-side (see `docs/push-notifications.md` + API_CHANGES_AND_FIXES.md). Gradle auto-applies google-services only when `android/app/google-services.json` exists, so build stays green without Firebase; push silently no-ops until that file + a Firebase project are added. No auto-unregister on logout (DELETE would be unauthenticated after token clear).
- **Mobile app (Capacitor/Android):** customer-only app shares the web codebase. `npm run build:mobile` (= `cross-env VITE_APP_TARGET=mobile vite build`) builds it; `IS_MOBILE_BUILD` (in `src/lib/platform.ts`) gates the `/admin` route in `App.tsx` and the Admin nav link in `Navbar.tsx`. The Admin lazy import is in a dead `IS_MOBILE_BUILD ? null : lazy(...)` branch so Rollup drops the Admin chunk from the mobile bundle entirely (verified: no `Admin-*.js` emitted). Native project in `android/`; sync with `npm run mobile:sync`, open with `mobile:open`, run with `mobile:run`. `capacitor.config.ts` → appId `com.kvsilverzone.app`, webDir `dist`. VITE_API_URL must point to a device-reachable server (emulator: `10.0.2.2`), not localhost.

- **Product size/weight variants:** `Product.variants?: ProductVariant[]` (`{ label, weight, height?, breadth? }`, all strings) in `src/context/CartContext.tsx`. Free-text `label` (S–XXL style); `height`/`breadth` are display-only. Normalized in `src/services/product.ts` (`normalizeVariants` trims + drops rows with no label/weight). Admin create+edit forms use a shared `VariantsEditor` component in `Admin.tsx` (rows stripped via `cleanVariants` before POST/PUT). `ProductDetail.tsx` shows a size selector that swaps the displayed weight/height/breadth AND recomputes `pricing.preGstPrice` for the selected variant (`computeProductPricing(product, silverRates, variantGrams)`). `ProductCard.tsx` lists available weights (`20g / 30g / 40g`). Backend persists/echoes `variants` on `/products`, `/products/:id`, `/admin/products` — contract in `docs/api/products-variants.md`.
  - **Variant weight now drives the actual charge, not just display (fixed 2026-07-05):** `handleAddToCart` in `ProductDetail.tsx` already baked the selected variant's weight+price into the cart item (`{...product, price: pricing.preGstPrice, weight: displayWeight}`), but `Payment.tsx` wasn't forwarding `item.weight` in the checkout payload — so the server (`pricing.service.ts`, which already supports a `weight`/`variantWeight` per-line override, see `computeCheckout`) recomputed using the product's BASE weight, silently charging the wrong amount for any non-default variant. Fixed by adding `weight: item.weight` to all three item-mapping call sites in `Payment.tsx` (`create-order`, `verify` for razorpay, `verify` for COD) and to the `RazorpayOrderPayload`/`PaymentVerificationPayload` types in `src/services/payment.ts`. Regression test: `kv-api/src/tests/pricing.service.test.ts` → `computeCheckout — variant weight passthrough`.

- **Product pricing config (fixed price + making charge + wastage):** `Product.isFixedPrice?: boolean`, `makingCharge?: ProductCharge`, `wastage?: ProductCharge` where `ProductCharge = { type: 'percentage'|'amount', value: number }` (`ChargeType` in `CartContext.tsx`). Normalized in `product.ts` (`normalizeCharge` — drops non-finite values, defaults unknown type to `percentage`). Admin forms (`Admin.tsx`) have a Fixed-Price `Switch` + two `ChargeInput` rows (type Select + numeric value) with inline validation via `validateCharge` (empty allowed; value ≥ 0; percentage ≤ 100); submit disabled while invalid. Fixed-price ON hides+omits making/wastage. `ProductDetail.tsx` hides the estimated price-breakdown block when `isFixedPrice`. making/wastage feed the dynamic price calc — NOT yet wired to cart total (next phase). Backend contract: `docs/api/products-pricing-config.md`. Convention: write ONE new md per API change in `docs/api/` (user preference, 2026-06-15).

- **Zone-based delivery charges:** `src/services/deliveryConfig.ts` — `DeliveryConfig { chennai, otherDistrict, otherState }` (₹, defaults 150/200/250), `resolveDeliveryZone({city,state})` (Chennai=home city → chennai; Tamil Nadu → otherDistrict; else otherState, case-insensitive), `getDeliveryCharge(config, address)`. Public `GET /delivery-config`; admin `GET`/`PUT /admin/delivery-config`. Admin UI is in `Admin.tsx` Shipping tab → "Delivery Charges" card (3 editable amounts + Save, validated ≥0). NOTE: an older in-memory "Delivery Fee Matrix" (pincode→rate, `pincodeRates` useState, NOT persisted/no API) still lives in the same tab — it's a mock; the zone card is the real one. Checkout money-path wiring (add zone charge to create-order `amount`/`breakdown` + replace "Shipping: Free" in `Payment.tsx`) is DEFERRED — see `docs/api/delivery-config.md` + API_CHANGES_AND_FIXES.md. Contract: `docs/api/delivery-config.md`.

- **Shop price filter:** `src/pages/Shop.tsx` has a two-handle price range **slider** (`PriceRangeSlider`, built on `@radix-ui/react-slider` directly because the shared `ui/slider.tsx` renders only one thumb and per CLAUDE.md ui primitives must not be hand-edited) plus the original preset radio buckets — they're mutually exclusive (`priceSlider` state overrides `selectedPriceRange`; each clears the other). Slider range is FIXED ₹100–₹5,000 (`PRICE_MIN`/`PRICE_MAX`/`PRICE_STEP=100` consts; was product-derived before 2026-06-16). Handles at the extremes ⇒ no cap on that end (`minPrice` undefined at floor, `maxPrice` undefined at ceiling), label shows "₹5,000+". `productService.getProducts` already accepts `minPrice`/`maxPrice` (frontend-only, no API change).
- **Test infra:** `src/test/setup.ts` stubs `window.matchMedia` AND now `ResizeObserver` (jsdom lacks it; Radix Slider needs it). Any new test rendering a Radix component that measures size relies on this stub — see [[bug-030]].

- **Docs layout (2026-06-16):** all project markdown was flattened into `docs/` and numbered oldest→newest (`01-...` … `21-...`); `docs/README.md` is the master index (chronological list + contract→client-file map). `plans/` and `docs/api/` folders were removed. Excluded/left in place: `README.md` (root), `CLAUDE.md`, `.claude/`, `.wolf/`. NOTE: older cerebrum entries and in-code comments may still reference pre-move paths like `docs/api/<name>.md` or root `API_CHANGES_AND_FIXES.md` — these now live under `docs/NN-*.md` (e.g. delivery-config → `docs/21-delivery-config.md`, products-variants → `docs/19-products-variants.md`, products-pricing-config → `docs/20-products-pricing-config.md`, api-changes-and-fixes → `docs/09-api-changes-and-fixes.md`). Use `docs/README.md` to resolve.

- **Multiple product images:** `Product.images?: string[]` (normalized srcs, ordered; `image` = primary = images[0]) in `CartContext.tsx`. `product.ts` `extractImages()` sorts API `images[{imageBase64,sortOrder}]` and normalizes, falling back to the flat `image`. Admin (`Admin.tsx`) uses a self-contained `ImagesUploader` (multi-file, thumbnail grid, remove + "make primary" reorder) in both forms; payload sends `image` (primary) + `images: string[]`. Sliding gallery via reusable `src/components/ProductImageCarousel.tsx` (embla `ui/carousel`) on `ProductCard.tsx` (hover arrows + dots) and `ProductDetail.tsx` (`alwaysShowControls`); falls back to plain `<img>` for a single image. IMPORTANT pattern: carousel lives inside ProductCard's `<Link>` — control buttons use `onClickCapture` with `e.preventDefault()+stopPropagation()` to avoid navigating (don't pass `onClick`, it would override embla's scroll handler). Write contract: `docs/22-products-multiple-images.md` (backend write side TBD).

- **Shop slider debounce + infinite scroll:** Price slider lives in a self-contained `PriceRangeFilter` component that owns the LIVE drag state locally (so dragging re-renders only that small component, NOT the whole Shop page/grid) and commits upward debounced (`PRICE_DEBOUNCE_MS` 700ms). Shop holds only the committed `priceFilter` (drives query key + min/max). `ProductCard` is `memo()`'d so the grid stays cheap on Shop re-renders. KEY PERF LESSON: the jank was NOT the debounce — it was per-tick `setState` in Shop re-rendering everything (the inline `FilterSidebar` component, rendered as `<FilterSidebar/>`, even remounts each render). Fix = isolate fast-changing UI state in a leaf component + memo heavy lists. Product list uses `useInfiniteQuery` (`PAGE_SIZE=12`, `initialPageParam:1`) with an `IntersectionObserver` sentinel (`rootMargin:400px`) for auto-load; results flattened + de-duped by id. `getNextPageParam` stops on empty/partial page, on a page LARGER than limit (unpaginated backend returned all), or when a full page repeats already-seen ids — so it degrades gracefully (shows all once, no dup fetches) until backend adds `page`/`limit`. `ProductFilters` gained `page?`/`limit?`. Contract: `docs/23-products-pagination.md`. Backend `/products` pagination NOT yet implemented.
- **Test infra:** `src/test/setup.ts` now also stubs `IntersectionObserver` (jsdom lacks it; infinite-scroll sentinel uses it) — alongside `matchMedia` + `ResizeObserver`. See [[bug-030]].

- **Dynamic product price calc (2026-06-16) — the "next phase" is now wired:** `src/lib/pricing.ts` is the shared engine. `computeProductPricing(product, silverRates, gramsOverride?)` → pre-GST price = metal value (`matchSilverRate` by purity × grams) + `makingCharge` + `wastage` (`resolveChargeAmount`: percentage on metal value, or flat amount). Fixed-price products skip the metal calc (flat `price` IS the pre-GST price) but discount/GST/delivery still apply. Goes "live" only when non-fixed AND rate+weight AND a making/wastage charge is configured; otherwise falls back to listed `product.price` (mirrors the backend guard so display == charged amount — legacy products with no charges keep their listed price rather than dropping to bare metal value, 2026-06-16). `computeOrderSummary({subtotal, taxableSubtotal, discount, gstPercent, deliveryCharge})` enforces the order **discount → GST on discounted taxable subtotal → delivery last**. Consumers: `ProductCard` (live price + adds computed price to cart), `ProductDetail` (selected-variant price + forward breakdown, replaced the old reverse-engineered 75/25 split; `parseGrams`/`parsePurityFraction` now imported from pricing.ts), `CartContext` (line prices are now PRE-GST; added `taxableTotal` to context), `Cart`/`Payment` (summaries show subtotal-before-GST + GST + delivery). **Prices shown in Shop are now pre-GST** (was GST-inclusive). Server stays authoritative for the charge; backend must mirror the model — contract in `docs/24-product-price-calculation.md`.

- **Daily rate-update guard — backend now live (2026-06-16):** Gold rates mirror silver (`src/services/goldRate.ts` → `/gold-rates/today|history`, `/admin/gold-rates`). The 10:00 IST cron persists an **authoritative** block flag at `GET /admin/rate-status` → `{ blocked, staleMetals[], checkedAt }` (`src/services/rateStatus.ts`, admin-only). `Admin.tsx` now PREFERS this flag over the client-side rule (`computeRateBlock`), which stays as the offline/staff fallback. Reconciliation lives in `resolveRateBlock` (`src/lib/rateFreshness.ts`): the server flag is the IST-correct trigger, but it's only recomputed by the cron, so a server-stale metal is dropped the instant the client's latest rate record for it is dated today — that's what clears the lock immediately on save (don't wait for next cron). Rate records expose the date as `rateDate` (silver) or both `date`+`rateDate` (gold); both services normalize `date ?? rateDate ?? createdAt` (see [[bug-144]]). Purity is pinned server-side (silver 999, gold 916/22K) — POST purity accepted but ignored, no round-trip needed. Spec/contract: `docs/25-price-update-guard-and-notification.md`. Staff carve-out for the rate endpoints/lock shipped 2026-07-05 (see decision log).
  - **Metal rate history table merged to one row per date (2026-07-05):** `Admin.tsx`'s Silver Rates tab previously flattened `[...silverRates, ...goldRates]` into one row PER RECORD, distinguished only by a raw `purity` value ("999" vs "916") that wasn't obviously readable as Silver vs Gold. Replaced with `metalRateHistory` — grouped by calendar day (`dayKey` normalizes both clean `YYYY-MM-DD` dates and full ISO timestamps via `new Date(...).toISOString().slice(0,10)`, since `date` can fall back to `createdAt` per bug-144) into `{dateKey, date, silver?, gold?}` rows. Table columns are now `Date | Silver Rate | Gold (22K) Rate | Updated By`, each metal cell showing `₹/g` (bold) + `₹/kg` (muted subtext) or `—` when that metal wasn't updated that day. "Updated By" merges both metals' `updatedBy` (shows once if the same person did both, `Silver: X · Gold: Y` if they differ).

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->
- [2026-06-14] A `validateForm` helper typed as a discriminated union `{success:true;data} | {success:false;errors}` failed TS narrowing under `if (!result.success)` at call sites (TS2339 on `.errors`). Fixed by returning a single shape `{ success: boolean; errors: Record<string,string> }` (errors empty when valid). Forms read `formData` directly, so the parsed `data` wasn't needed.
- [2026-06-14] Bug-029: `Shop.tsx handleSearchChange` called `setSelectedPriceRanges([])` (plural) — setter is `setSelectedPriceRange('')` (singular string). Typing in search threw ReferenceError. Verify setter names match useState declarations.
- [2026-06-14] Adding `<Seo>` (react-helmet-async) to a page broke `Shop.test.tsx` with "Cannot read properties of undefined (reading 'add')" because the test render had no `<HelmetProvider>`. Fix: wrap page-render test helpers in `<HelmetProvider>`. Applies to any future test rendering a page that uses `<Seo>`.
- [2026-07-05] `services/product.ts`'s `ApiProduct extends Partial<Omit<Product, 'id'>>` redeclared `images` as `ProductImage[]` while the inherited `Product.images` is `string[]` — TS2430 ("incorrectly extends"). Whenever a raw API shape interface extends the normalized frontend type and needs a field in a different raw shape (objects vs. normalized strings), `Omit` that field out of the base too: `Omit<Product, 'id' | 'images'>`, not just `'id'`.
- [2026-07-05] Backend `pricing.service.ts` already supported a per-line `weight`/`variantWeight` override (parsed via `parseWeight`) to price a checkout line at a selected size-variant's weight instead of the product's base weight — but the frontend never sent it. `Payment.tsx`'s three checkout item-mapping call sites (`create-order`, `verify` for razorpay, `verify` for COD) only sent `{product, quantity}`; a customer who picked a heavier/lighter variant on `ProductDetail` would be shown the correct variant price in the cart, but CHARGED based on the base product weight at checkout — a silent price mismatch, not caught by the amount-verification step since it recomputes with the same (wrong) weight both times. Always check that a backend "override" field a display page already computes locally is actually forwarded through every checkout payload, not just used for local display math.
- [2026-07-05] `AuthContext.tsx` `login`/`signup` wrote the raw JWT to `localStorage[TOKEN_STORAGE_KEY]` unconditionally on EVERY platform, even though `api.ts` only ever reads it when `isMobileApp()` — on web the cookie is the real session and the localStorage copy was pure unused XSS exposure. Fixed: gate the write on `isMobileApp()`, and purge any stray token from localStorage on web at `initAuth()` startup (covers users already logged in from before the fix). Lesson: when a field is genuinely needed on only one platform/build target, gate the WRITE, not just the read — writing "just in case" leaves sensitive data sitting in a JS-readable store for no reason.

- [2026-07-05] **Admin panel tab routing:** `Admin.tsx`'s `Tabs` is now URL-controlled instead of `defaultValue`-only local state. `App.tsx` mounts it at `path="/admin/*"` (was bare `/admin`); `Admin.tsx` reads `useParams()['*']` as the active tab (empty splat → `'dashboard'`), and `Tabs`' `onValueChange` calls `navigate(\`/admin/${value}\`)`. `ADMIN_TABS` const lists all 12 valid segments; `ADMIN_ONLY_TABS = ['theme', 'inventory']` are hidden from staff in the `TabsList` AND redirect to `/admin/dashboard` if a staff user hits their URL directly (`isTabAccessible` check, right after the `rateBlock.blocked` early-return so the mandatory-rate lock still takes priority). An unknown tab segment (e.g. `/admin/bogus`) also redirects to dashboard. Verified live via Playwright: deep links, tab-click URL sync, browser back/forward, and both redirect cases all work.
- [2026-07-05] **Product-owner audit of the admin panel found the staff role almost entirely non-functional** (browser-verified: 403 on nearly every `/admin/*` GET as soon as a staff user opened the panel — stats, orders, users, savings, coupons, returns, inventory, delivery-config — despite the frontend clearly building all those tabs for staff already, `enabled: role==='admin'||'staff'` on the queries, no per-action role gating on the buttons). Root cause was backend-side (`kv-api` blanket `admin`-only middleware) — see [[kv-api decision log]] 2026-07-05. Fixed in lockstep: backend now allows staff on all of those; frontend inventory-only queries changed to `enabled: role === 'admin'` (product decision: staff mirror admin except inventory) and the Inventory tab hidden from staff (`ADMIN_ONLY_TABS`).
- [2026-07-11] **React onClickCapture + onClick on the SAME element: never call `stopPropagation()` in the capture handler if a sibling `onClick` on that element must still fire.** React collects capture- and bubble-phase listeners on one element into a single ordered dispatch sequence; `stopPropagation()` called during the capture-phase listener halts that sequence before the element's own bubble-phase `onClick` runs — this is NOT the same as stopping propagation to ancestors only. Bit us in `ProductImageCarousel.tsx` (bug-152): CarouselPrevious/CarouselNext got `onClickCapture={stopNav}` (to block the surrounding `<Link>` navigation) while the shadcn carousel primitive set its own `onClick={scrollPrev/scrollNext}` internally — the arrows silently did nothing. Fix: if you only need to stop a `<Link>`/anchor from navigating, `e.preventDefault()` alone is enough (React Router's `Link` explicitly checks `event.defaultPrevented`) — don't reach for `stopPropagation()` unless you've confirmed no sibling handler on the same element needs to run after it. When a click handler must both suppress navigation AND perform an action, prefer doing both in ONE handler (like the carousel's dot indicators do) rather than splitting across onClickCapture/onClick.
- [2026-07-05] **Confirmed-but-unfixed findings from the same audit** (reported to user, not yet fixed — beyond the scope of the routing+RBAC pass): (1) Shipping tab's "Delivery Fee Matrix" card (pincode→rate, `pincodeRates` useState) is pure mock — never persisted, never read by checkout (the real zone-based `DeliveryConfig` card above it is what actually charges customers); the card's own copy ("Checkout will cross-reference the customer's destination pin") is false. (2) Filters tab (Category Visibility / Metal Filter Options / Price Range Options — NOT the "Manage Categories" card, which is real) persists only to `localStorage['kv-filter-config']`; `Shop.tsx` reads the same key, so it only works same-browser — real customers on other devices never see an admin's filter changes, even though a backend `GET/PUT /admin/filter-config` exists unused. (3) No admin UI exists for `pricing-config` (GST%) or `gift-vouchers` at all, despite full backend CRUD. (4) `lowStockItems`/`inventorySummary` are fetched (real API) but never rendered anywhere in the Inventory tab JSX — dead query, and a real low-stock-alert feature request going unserved. (5) The header "Settings" button has no `onClick`. (6) Customers table role badge only checks `u.isAdmin` (shows "Customer" for staff rows) — doesn't use `getUserRole`. (7) Edit-Product modal shows Price/Original-Price fields unconditionally, unlike the Create-Product dialog which correctly hides them for non-fixed-price (dynamic) products. (8) Inventory ledger's "Status Impact" column flags "Low Stock" by `tx.quantity < -5` (single-transaction size) instead of actual remaining stock — disconnected from the real low-stock query in (4). Also confirmed separately: `PUT/DELETE /admin/users/:id`, `DELETE /admin/orders/:id`, and category add/delete all 404 for every role, not staff-specific — see [[kv-api decision log]] 2026-07-05.

- [2026-07-20] **Business-owner requirements audit (list #1 of 2) — ALL items below were fixed same-day, see next entry for what shipped.** Original findings (historical, now resolved): (1) Razorpay online checkout can fail order creation whenever delivery charge applies — `RazorpayOrderPayload` (`src/services/payment.ts`) only sends `pincode`, not city/state, so `create-order`'s amount is computed with delivery=₹0, but `verify` sends the full address and delivery gets included, causing a charged-vs-computed mismatch (`kv-api/src/services/payment.service.ts:91-121`) that throws and fails order creation after the customer already paid the lower amount. COD unaffected (skips this cross-check). Fix by sending shippingAddress (not just pincode) to create-order too. (2) `PassbookView.tsx` is fully built (real passbook numbers, deposit ledger) but never imported anywhere — dead code; "View Passbook" button in `SavingsScheme.tsx` has no onClick. (3) Offline-stall signup coupon is completely non-functional: `Signup.tsx` never forwards `stallEvent` through `AuthContext`→`auth.ts`→`POST /auth/signup`, so the backend's auto-coupon logic (`user.service.ts`, real code, ties into `coupon.model.ts`) never fires — yet the UI shows a "coupon credited" success toast regardless. Admin's stall-mode toggle is also just a `localStorage` flag, not server-enforced. (4) `/gift-vouchers` page is fully correct (GST-inclusive, not double-taxed) but has zero links from Navbar/Footer — direct-URL only. (5) No savings "look up by passbook number" endpoint exists despite passbook numbers being generated; frontend `SavingsEnrollment` type doesn't even declare `passbookNumber` (accessed via `as any`). (6) WhatsApp integration (`kv-api/src/utils/whatsapp.ts`) sends exactly ONE message type in the whole codebase: an internal alert to a single admin number when the daily 10am rate update is missed — no installment reminders, payment-success confirmations, festival promos, birthday/anniversary wishes, and no success-case notification when the rate update DOES happen on time. (7) No OTP login or OTP password-reset flow exists anywhere (earlier grep hits for "otp" were false positives inside "forgotPassword"); the Shadcn `input-otp.tsx` primitive is unused. (8) Invoice generation is print-only reformatting of the order (`InvoiceView.tsx` + react-to-print) — no backend invoice model/number sequence, no GSTIN. (9) **Discrepancy flagged for owner sign-off, not a bug**: owner's spec says product weight/height/breadth variants are "only for customer understanding" (display-only), but the 2026-07-05 variant-weight fix (see entry above) made variant weight genuinely drive the charged amount — these two now contradict each other, needs explicit clarification on intended behavior.

- [2026-07-20] **Business-owner audit fixes shipped (all 12 items from the 2026-07-19 audit) — new subsystems and where they live:**
  - **Razorpay delivery-charge bug (critical, live-money fix):** `Payment.tsx`'s `handleRazorpayPayment` now sends `shippingAddress` (not just `pincode`) to `createRazorpayOrder` — the backend already accepted it (`payment.controller.ts`), the frontend just never sent it, so create-order priced delivery at ₹0 while verify included it, causing every online order with a delivery charge to fail post-payment. Type added to `RazorpayOrderPayload` in `src/services/payment.ts`.
  - **Server-authoritative singleton configs**, all following the same model/repository/routes pattern as `DeliveryConfig` (`kv-api/src/models/{stallConfig,invoiceConfig}.model.ts`, matching repositories, public routes `stallConfig.routes.ts`/`invoiceConfig.routes.ts`, admin GET/PUT in `admin.routes.ts`, frontend services `src/services/{stallConfig,invoiceConfig}.ts`): (1) **Offline-stall mode** — replaces the old `localStorage['kv-offline-stall']` toggle in `Admin.tsx`; `Signup.tsx` now queries public `GET /stall-config` and only shows the stall banner when `?stall=1` AND the server flag is active; `user.service.ts signup()` re-checks the flag server-side before honouring `stallEvent` so it can't be spoofed by POSTing the flag directly. (2) **Invoice/GSTIN settings** — company name/GSTIN/address/phone/email, editable in Admin.tsx's Shipping tab, consumed by `InvoiceView.tsx` (self-fetches via `invoice-config` query) to print a real GSTIN header instead of nothing.
  - **Order invoice numbering:** `Order.invoiceNumber` (`INV-{year}-{seq:6}`, per-calendar-year sequence) generated in `order.repository.ts create()`, mirroring the existing `Savings.passbookNumber` pattern. `InvoiceView.tsx` uses it (falls back to the old order-id-slice scheme for pre-existing orders) and now shows a dynamic GST% (computed from `tax/subtotal`) instead of a hardcoded "3%".
  - **Savings passbook, now actually reachable:** `PassbookView.tsx` was fully built but never imported anywhere (dead code) — now wired into `SavingsScheme.tsx` via a Dialog + `useReactToPrint`, triggered by the "View Passbook" button. Added `GET /savings/passbook/:passbookNumber` (owner-only unless admin/staff — `savings.service.ts getByPassbookNumber`) + a "Track by passbook number" search box on `SavingsScheme.tsx`; `CustomerDashboard.tsx`'s Passbook button now deep-links to `/savings-scheme?passbook=...` (auto-opens the dialog via a `useEffect` on `searchParams`) instead of a dead anchor-scroll. `SavingsEnrollment` type (`src/services/savings.ts`) now declares `passbookNumber`/`payments` for real instead of `(scheme as any).passbookNumber` casts scattered across 3 files.
  - **Savings minimum-payment floor:** `savings.service.ts recordPayment()` now rejects `amount < scheme.monthlyAmount` (previously only enforced at `enroll()`, so any post-enrollment installment could underpay silently).
  - **WhatsApp is now a real multi-purpose notifier, not just the one rate-alert message** (`kv-api/src/utils/whatsapp.ts` gained `sendPaymentSuccessMessage`, `sendSavingsPaymentSuccess`, `sendSavingsReminder` (day1/day5/day10/missed), `sendRateUpdateSuccessNotice`, `sendBirthdayWish`, `sendAnniversaryWish`, `sendBroadcast`): payment-success fires from `payment.service.ts verifyAndCreateOrder` (order) and `savings.service.ts recordPayment` (installment); rate-update success fires from `rateGuard.service.ts` **only** when `notifyOnSuccess=true` is passed — gated so it fires from the scheduled 10 AM cron only, never from the server-startup or on-demand `checkAndNotify()` calls (would otherwise spam the ops number on every restart). Two new daily crons in `server.ts`: `SavingsReminderService` (9:00 IST — day1/5/10 overdue + a single "missed" notice at day 15, computed from `startDate + installmentNumber months`) and `BirthdayWishService` (9:30 IST — matches `User.dateOfBirth`/`anniversaryDate` by month+day only, year ignored). Admin festival-broadcast tool: `POST /admin/whatsapp/broadcast` (sequential sends, not `Promise.all`, to respect Cloud API rate limits) + a textarea+button card in `Admin.tsx`'s Shipping tab.
  - **User model gained `dateOfBirth`/`anniversaryDate`** (optional Date fields), editable on `Profile.tsx` (blank string is stripped before `updateProfile()` — Mongoose can't cast `''` to Date).
  - **OTP login (business decision, 2026-07-19): email now (Brevo), WhatsApp once Meta approves an Authentication template.** New `OtpCode` model (`kv-api/src/models/otpCode.model.ts`, TTL-indexed on `expiresAt`) + `OtpService` (`request/verifyLoginOtp`): 6-digit code via `crypto.randomInt` (not `Math.random`), bcrypt-hashed at rest, 5-attempt lockout, 5-min expiry (`config.otpExpiryMinutes`). `POST /auth/otp/request` always returns a generic "if that email is registered..." message (no user enumeration) and only actually emails a code when the account exists; `POST /auth/otp/verify` sets the same httpOnly cookie as password login. WhatsApp send is gated behind `config.whatsappOtpEnabled` (`WHATSAPP_OTP_ENABLED` env, default false) so it silently no-ops until Meta template approval — flip the env var, no code change needed. `Login.tsx` now has a Password/OTP mode `Tabs` toggle; `AuthContext.loginWithOtp(email, code)` mirrors `login()`'s cookie/localStorage handling.
  - **Variant-weight pricing decision (2026-07-19, confirmed with owner):** keep the current behavior — selected variant weight DOES drive the charged amount (the 2026-07-05 fix stays as-is). The owner's written spec saying variants are "only for customer understanding" was superseded; no code change was needed here, just documenting the resolved discrepancy so it isn't re-flagged.
  - All 4 nav gaps closed: `/gift-vouchers` and `/dashboard` (as "My Dashboard") added to `Navbar.tsx`'s `navLinks`/dropdown; `/gift-vouchers` added to `Footer.tsx`.
  - Verified clean: `kv-api` `tsc --noEmit` + `npm run build` + full Jest suite (79 tests) all pass; `kv-silver-zone` `tsc --noEmit` + `npm run build` + full Vitest suite (82 tests) + `npm run lint` all pass with zero new errors introduced.

- [2026-07-20] **Second audit batch (5 items) shipped:** (1) Added "18K Gold" to the product purity `Select` in both Admin create/edit forms — **flagging a pre-existing, unrelated pricing gap found while doing this**: `resolvePurityFraction`/`resolveChargeAmount` in both `pricing.ts` (frontend) and `pricing.service.ts` (backend) parse "22K"/"18K" as a literal `digits/100` fraction (0.22 / 0.18) — NOT actual karat purity (22/24≈0.916, 18/24=0.75) — AND `computeCheckout`/`computeProductPricing` always price every product off the SILVER rate (`getCurrentSilverRatePerGram`), never the gold rate, regardless of `product.material`. So any gold product using dynamic (non-fixed) pricing today computes a badly wrong charge. Not fixed (out of scope of "add the option") — gold products should stay `isFixedPrice: true` until this is addressed; ask the user if they want a proper gold-aware pricing engine built. (2) `/silver-rate` never showed gold: it only ever queried `silverRateService` (`/silver-rates/*`), and gold rates live in a separate `/gold-rates` collection since the metals were split (see the 2026-06-16 "Daily rate-update guard" entry above) — fixed by also querying `goldRateService` and merging. Note: `Marquee.tsx` (the site-wide header ticker) has the exact same latent bug (only fetches `silverRateService`) — NOT fixed this pass since only `/silver-rate` was in scope; worth doing in the same sweep next time. (3) New `FloatingRateWidget.tsx` — fixed bottom-right, non-dismissible, Home-page-only (mounted in `Index.tsx`), links to `/silver-rate`. (4) `/silver-rate` gained a full-bleed auto-scrolling ticker at the top (reuses the `.marquee` CSS keyframe from `index.css`, same duplicate-content-for-seamless-loop technique as `Marquee.tsx`) with genuinely dynamic green/red arrows computed from `getChange()` — unlike `Marquee.tsx`'s ticker, which hardcodes `icon:'up'` (green) regardless of actual direction. (5) COD removed: `Payment.tsx`'s Payment Method card is now a static Razorpay-only info block (no `RadioGroup`), `handlePayment` always calls `handleRazorpayPayment`; backend `payment.service.ts verifyAndCreateOrder` now throws 400 on `paymentMethod === 'cod'` (case-insensitive) before the signature-check branch, so COD can't be forced via direct API calls either. Note: `POST /orders` (`order.routes.ts` → `OrderService.createOrder`) still accepts `paymentMethod: 'cod'` and its test (`admin-orders.api.test.ts`) is untouched — confirmed via `orderService.createOrder` grep that this endpoint is DEAD CODE, never called from the frontend (live checkout only goes through `/payments/create-order` + `/payments/verify`), so it was deliberately left alone rather than risk touching an unrelated path.
  - Returns policy ("only if KV's fault, video required") was raised as an open discussion, not implemented — current `Return` model (`kv-api/src/models/return.model.ts`) has no fault-category or video/evidence field at all, just free-text `reason`. See conversation for the recommendation given (fault-tiered reasons + mandatory unboxing video + shortened claim window); implement only once the user picks a direction.

- [2026-07-20] **Returns overhaul shipped (fault-tiered + WhatsApp-video-verified claims).** Discovered while building it: there was NO customer-facing "file a return" UI anywhere before this — `returnsService.createReturn` was never called from any page (only a read-only "Return Requests" list existed in `Profile.tsx`); built the actual request flow from scratch.
  - **Data model:** `Return` gained `faultType` (`kv_fault` | `customer_preference`, required), `videoStatus` (`not_required`|`awaiting`|`received`), `videoReferenceCode` (`RET-XXXXXX`, generated at creation by pre-assigning the Mongo `_id` before `.save()` — same trick as `Order.invoiceNumber`/`Savings.passbookNumber`), `videoFilePath`/`videoMimeType`/`videoReceivedAt`/`videoSenderPhone`. Also fixed a silent pre-existing bug: `description` was accepted by the API but never persisted (`return.repository.ts create()` dropped it) — now saved.
  - **Claim window:** `Order` gained `deliveredAt` (stamped once, first transition to `'Delivered'`, in `order.repository.ts updateStatus()` — never overwritten on subsequent saves). `return.service.ts createReturn()` rejects with 400 if the order isn't Delivered or is outside `config.returnClaimWindowHours` (default 48, env `RETURN_CLAIM_WINDOW_HOURS`).
  - **WhatsApp video pipeline (customer's explicit ask — "send to WhatsApp for server storage" instead of a web upload):** inbound webhook at `/api/v1/webhooks/whatsapp` (`whatsappWebhook.routes.ts` + `.controller.ts`) — GET does Meta's verify-token handshake, POST receives messages and verifies `X-Hub-Signature-256` via `config.whatsappAppSecret` (best-effort/warns if unset, matching the rest of the WhatsApp integration's graceful-degradation style). Raw body captured for the HMAC check via `express.json({verify: ...})` in `app.ts` (stashes `req.rawBody`, harmless on every other route). Media download is `utils/whatsappMedia.ts` (2-step Graph API: media-id → signed URL → bytes, raw `https`, same style as `utils/whatsapp.ts`) — **stored to LOCAL DISK** under `uploads/return-videos/` (gitignored), no cloud storage integration exists in this project at all (images are base64-in-Mongo, nothing else). **Flagged as a known limitation**: this assumes a single long-running server process with a persistent filesystem — won't survive a serverless/ephemeral/multi-instance deployment without swapping to S3 or similar.
  - **Matching logic** (`return.service.ts handleIncomingWhatsAppMedia`): (1) regex `RET-[A-Z0-9]{6}` in the video caption → exact match if that return is `videoStatus:'awaiting'`; (2) else, sender's WhatsApp number matched against the **order's** `shippingAddress.phone` (not `user.phone` — more reliable, always present) across all `awaiting` returns, auto-matched only if there's exactly one candidate; (3) else stored as `UnmatchedReturnVideo` (new model) for manual admin reconciliation. Admin Returns tab gained a "Video" column (link when received, code when awaiting) + an "Unmatched WhatsApp Videos" card (view video, pick a pending return from a `Select`, "Link" button → `POST /admin/return-videos/unmatched/:id/link`).
  - **Customer flow:** `Profile.tsx` Orders tab → "Request Return" button (Delivered orders only) → Dialog: item checkboxes, faultType `RadioGroup` (kv_fault vs customer_preference, with inline copy explaining refund vs exchange-only), reason/description. On submit, `kv_fault` claims show the WhatsApp number + reference code + window inline (from `createReturn`'s response `videoInstructions`, not a second round-trip); `customer_preference` shows an exchange/store-credit explanation. Returns tab list now shows faultType + videoStatus badges.
  - **Policy copy updated everywhere it was stale:** `Index.tsx` trust bar ("7-day hassle-free" → "Fault-Protected Returns, Video-verified 48-hr window"), `FAQ.tsx` (3 Q&As rewritten), `TermsConditions.tsx` §5, `ProductDetail.tsx`'s "Returns & Exchange" info list. All previously advertised a blanket 7-day no-questions-asked policy that no longer reflects reality.
  - **Setup required before this is live**: env vars `RETURN_VIDEO_WHATSAPP_NUMBER` (falls back to `RATE_ALERT_RECIPIENT` if unset — set a real dedicated number), `WHATSAPP_WEBHOOK_VERIFY_TOKEN` + `WHATSAPP_APP_SECRET` (register the webhook URL `https://<api-host>/api/v1/webhooks/whatsapp` in Meta App Dashboard → WhatsApp → Configuration, subscribe to the `messages` field), optionally `RETURN_CLAIM_WINDOW_HOURS` / `RETURN_VIDEO_STORAGE_DIR`. Without `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_ID` (existing config) media download will fail — same prerequisite as the rest of the WhatsApp integration.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->
- [2026-04-05] Implemented a dedicated `ChangePassword` page for normal users and a separate admin-only modal action in the customers table, both hitting the same endpoint for consistent backend behavior.
- [2026-04-05] Switched theme persistence to backend store-config endpoints for both public app initialization and admin save flow.
- [2026-06-16] Product price is computed **live on the frontend** (silver rate × weight + making + wastage) for display in Collections/Detail, overriding any stale backend `product.price` — user chose this over "backend already computes it". Prices shown are **pre-GST**; GST + delivery are added at checkout. The actual charged amount stays **server-authoritative** (`create-order` returns it; no client total sent) — user chose "fix display only + document backend" over wiring the computed total as the charge, to avoid frontend/backend drift on the money path.
- [2026-06-14] Chose Capacitor (over React Native rewrite or PWA-only) to ship a customer-only Android app, because admin is isolated to one route so the existing React SPA could be reused near-100%. Kept it in the same repo behind a `VITE_APP_TARGET=mobile` build flag (vs a separate mobile config or repo fork) for one source of truth. Mobile auth uses Bearer tokens instead of the httpOnly cookie because the Capacitor WebView origin can't share the web cookie cross-origin.
