services:
  kvs-web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://api.kvsilverzone.com/api/v1}
    container_name: kvs-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
